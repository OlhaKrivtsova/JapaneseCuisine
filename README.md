Japanese Cuisine: the learning project in React.
Implemented the shopping cart, checking the ordering form and sending the order to the database. 

Tech: React | CSS Modules| Fetch API | Firebase API 
